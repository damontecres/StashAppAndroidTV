fragment SlimTagData on Tag {
  id
  name
  description
  favorite
  image_path
}

fragment TagData on Tag {
  id
  name
  description
  favorite
  aliases
  scene_count
  performer_count
  scene_marker_count
  image_path
  image_count
  gallery_count
  parent_count
  child_count
}

fragment JobData on Job {
  id
  status
  progress
  description
  subTasks
  addTime
  endTime
  error
}
