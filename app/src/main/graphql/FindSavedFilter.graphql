query FindSavedFilter($id: ID!) {
  findSavedFilter(id: $id) {
    id
    mode
    name
    find_filter {
      q
      page
      per_page
      sort
      direction
      __typename
    }
    object_filter
    ui_options
    __typename
  }
}
